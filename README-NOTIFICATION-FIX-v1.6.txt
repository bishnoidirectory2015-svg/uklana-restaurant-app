Uklana Restaurant App v1.6

Only notification alert behavior changed:
- new loud notification channel v2
- old v1 channel recreated for existing server payloads
- bundled order_alert.wav used as reliable default ringtone
- high priority, vibration and full-screen/heads-up intent
- lock-screen public visibility

After installing the update, open the app once so Android creates the new channels.
